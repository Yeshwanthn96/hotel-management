server:
  port: ${PORT:8094}

spring:
  application:
    name: PAYMENT-SERVICE

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://hotel_db_user:bpR7axsHo7abot4r@hotelmanagement.yzu70xs.mongodb.net/hotel_payments?retryWrites=true&w=majority}
      auto-index-creation: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    enabled: ${EUREKA_ENABLED:true}

# Stripe Configuration
stripe:
  api:
    key: ${STRIPE_API_KEY:sk_test_demo_key_for_testing}
  publishable:
    key: ${STRIPE_PUBLISHABLE_KEY:pk_test_demo_key_for_testing}
