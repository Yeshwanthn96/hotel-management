
server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:./config-repo
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
